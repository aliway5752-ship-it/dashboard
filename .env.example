# Remote Bot Express API (public URL — used server-side by Next.js API routes)
NEXT_PUBLIC_BOT_API_URL=http://your-vps-ip:4000

# Secret API key — server-side only, never exposed to the browser
BOT_API_KEY=your-secret-api-key
